{
  "name": "pausenplay",
  "version": "1.0.0",
  "private": true,
  "description": "PausenPlay gaming lounge — customer site with live seat booking + admin console",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mongodb": "^6.0.0",
    "razorpay": "^2.9.8"
  }
}
